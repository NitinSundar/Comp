#include<bits/stdc++.h>
using namespace std;
const int N = 4e5 + 5;
vector<double> a(N);
int main()
{
	cout<<fixed;
	cout<<setprecision(12);
	int n;
	double k;
	cin>>n>>k;
	for(int i=0;i<n;i++)
		cin>>a[i];
	sort(a.begin(),a.end());
	do
	{
		
	}while(b.size() > k)

	return 0;
}