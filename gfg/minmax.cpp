#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;

int main() 
{
    int T;
    cin>>T;
    
    while(T--)
    {
       int n;
       cin>>n;
       int mi = 1e9,ma = -1;
       int xl
       for(int i=0;i<n;i++)
       {
            cin>>x;
            ma= max(ma,x);
            mi= min(mi,x);
       } 
       cout<<ma<<" "<<mi<<"\n";

    }   
    return 0;
}